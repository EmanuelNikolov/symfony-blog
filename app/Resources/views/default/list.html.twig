{% extends 'base.html.twig' %}

{% block body %}
    <h1>List of articles</h1>
    {% for article in articles %}
        <li><a href="{{ path('view_article', {'id': article.id}) }}">{{ article.title }}</a> - {{ article.createDate|date('Y-m-d') }}</li>
    {% endfor %}
{% endblock %}

